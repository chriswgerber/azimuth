#compdef awslogin

# Awslogin Completion
_awslogin() {
  tag_file=~/.aws/instance_tags.yml
  if [ -f $tag_file ];
  then
    _arguments -C "1: :(`awk '/-/ {if ($1 == "-") print $2}' $tag_file`: )";
  else
  fi
}
